{
  "name": "atma",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "bun run --filter @atma/backend dev",
    "dev:dashboard": "bun run --filter @atma/dashboard dev",
    "dev": "bun run --filter '*' dev",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "typescript": "^6.0.3"
  }
}
